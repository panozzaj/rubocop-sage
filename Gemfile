# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

gem 'rake'
gem 'rspec', '~> 3.0'
gem 'rubocop', '>= 1.0'
gem 'rubocop-rspec', '>= 2.0'

# Needed so requires_gem declarations work during testing
gem 'activerecord'
gem 'capybara'
